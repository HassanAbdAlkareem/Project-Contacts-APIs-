import React from "react";
import ReactDOM from "react-dom";
import AppJsonServer from "./component/AppJsonServer";
ReactDOM.render(
  <React.StrictMode>
    <AppJsonServer />
  </React.StrictMode>,
  document.getElementById("root")
);
